@model asp_autonews.Domain.Entities.InfoField
@{
    // из панели администратора
    ViewBag.Title = Model.MetaTitle;
    ViewBag.Description = Model.MetaDescription;
    ViewBag.Keywords = Model.MetaKeywords;
}

<div class="text-center">
    @Html.Raw(Model.Text)
</div>
